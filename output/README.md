# output/

Where finished invoices land, one file per run. Empty until the translator runs.

Nothing here is hand-edited to move the pipeline forward: the invoice is generated, a person reads it, and only then is it numbered in the register and sent. This folder is the product; everything above it is the factory.
